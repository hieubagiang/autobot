__pycache__/
*.pyc
*.pyo
.env
*.log

# Không commit file accounts (chứa email/password)
accounts.txt
accounts_2.txt
DONE|*
.chrome_profiles/
.idea/

# Xeca runtime watchlist state (differs per-environment, not source)
state.json
state.json.tmp

# cinema_booking runtime watchlist state (differs per-environment, not source)
cinema_booking_state.json
cinema_booking_state.json.tmp

# TQTT registrants chứa CCCD/SĐT/email THẬT — không commit, xem data/tqtt_registrants.example.json
data/registrants.json

# TQTT field-key mapping cache (runtime, re-derived from the live frontend bundle each run)
data/tqtt_field_keys_cache.json

# crypto_signals runtime state + Telethon session (differs per-environment, not source;
# the .session file is a live credential -- never commit it)
crypto_signals_state.json
crypto_signals_state.json.tmp
crypto_signals_state.json.lock
crypto_signals_session.session
crypto_signals_session.session-journal
